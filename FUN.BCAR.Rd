% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FUN.BCAR.R
\name{FUN.BCAR}
\alias{FUN.BCAR}
\title{The FUN.BCAR function}
\usage{
FUN.BCAR(
  data,
  group.var,
  categorical.covariates,
  continuous.covariates,
  group.level,
  ratio,
  categorical.weights,
  continuous.weights,
  start.number,
  num.categories,
  pct.deterministic,
  planned.sample.size
)
}
\arguments{
\item{data}{A data frame that contains the covariates to be randomized.}

\item{group.var}{The name of the grouping variable. The default is group.}

\item{categorical.covariates}{A list of categorical covariates to be balanced.}

\item{continuous.covariates}{A list of continuous covariates to be balanced.}

\item{group.level}{A list of all potential group ids.}

\item{ratio}{The assignment ratio for groups listed in group.level.}

\item{categorical.weights}{A list of weighting factors for the categorical covariates.}

\item{continuous.weights}{A list of weighting factors for the continuous covariates.}

\item{start.number}{From which subject that the continuous covariates will be balanced. The default is 15.}

\item{num.categories}{The number of categories preset for the categorical variable that a continuous covariate will be converted to. The defalut is 4.}

\item{pct.deterministic}{The percentage of subjects that will be assigned to a group in a more deterministic manner.}

\item{planned.sample.size}{The planned sample size for the study.}
}
\description{
The FUN.BCAR function
}
\examples{
data = data.frame("gender" = sample(c("female", "male"), 100, TRUE, c(.5, .5)),
                  "age" = sample(c("<=50", ">50"), 100, TRUE, c(.5, .5)))
data$continuous1=rnorm(nrow(data))
data$continuous2=rnorm(nrow(data))

data1=data[c(1,2),]
data1$group=FUN.BCAR(data1, group.var="group", categorical.covariates=c("gender", "age"),
continuous.covariates=c("continuous1", "continuous2"), group.level=c("A", "B", "C"))

data2=data[-c(1,2),]
data2$group=NA
datanew=rbind(data1, data2)
datanew$group=FUN.BCAR(datanew, group.var="group", categorical.covariates=c("gender", "age"),
continuous.covariates=c("continuous1", "continuous2"), group.level=c("A", "B", "C"), 
planned.sample.size=100)

#table(datanew$gender, datanew$group)
#table(datanew$age, datanew$group)
#table(datanew$group)
#boxplot(datanew$continuous1~datanew$group)

}
